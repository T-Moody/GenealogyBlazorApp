{
  "name": "PROJECT_NAME_HERE",
  "version": "0.1.0",
  "description": "Short description of the application's purpose goes here.",
  "type": "Blazor Server-Side Rendering (SSR) combined with WebAssembly (WASM)",
  "framework": {
    "name": ".NET",
    "version": "10.0"
  },
  "languages": ["C#", "Razor", "JavaScript", "CSS"],
  "mainFiles": [
    "Program.cs",
    "DEVELOPMENT_LEDGER.md",
    "ARCHITECTURE.md"
  ],
  "importantDirectories": [
    "Features",
    "Domain",
    "Infrastructure",
    "Shared",
    "Components",
    "Pages",
    "tests"
  ],
  "conventions": {
    "namingConventions": {
      "services": "PascalCase with 'Service' suffix (e.g., ExampleService)",
      "repositories": "PascalCase with 'Repository' suffix (e.g., ExampleRepository)",
      "components": "PascalCase (e.g., ExampleComponent)",
      "endpoints": "PascalCase with 'Endpoint' suffix (e.g., GetExampleEndpoint)",
      "handlers": "PascalCase with 'Handler' suffix (e.g., GetExampleHandler)",
      "commandsQueries": "PascalCase with 'Command' or 'Query' suffix (e.g., CreateExampleCommand)"
    },
    "patterns": {
      "architecture": "Vertical Slice Architecture (feature-based folders)",
      "dataAccess": "EF Core via DbContext and repository/extension methods where appropriate",
      "validation": "FluentValidation for commands and queries",
      "logging": "Structured logging with placeholders for parameters"
    },
    "testing": {
      "framework": "xUnit for unit tests, bUnit for Blazor UI tests",
      "location": "PROJECT_NAME_HERE.Tests/",
      "coverage": "Target: meaningful coverage on core features (handlers, validators, critical components)"
    }
  },
  "quickStart": {
    "setup": [
      "dotnet restore",
      "dotnet ef database update   // if using EF Core migrations",
      "dotnet run"
    ],
    "test": "dotnet test",
    "build": "dotnet build"
  },
  "recentFeatures": [
    {
      "date": "YYYY-MM-DD",
      "name": "FEATURE_NAME_PLACEHOLDER",
      "description": "Brief description of a recently added feature."
    }
  ],
  "keyServices": [
    "ExampleService - High-level domain/application operations description placeholder",
    "AnotherService - Another important service description placeholder"
  ],
  "databaseProvider": "SQL Server",
  "authentication": "Authentication strategy placeholder (e.g., None for MVP, Identity, OAuth, etc.)",
  "realtime": "Optional: SignalR or other realtime technology (or 'None' if not used)"
}
